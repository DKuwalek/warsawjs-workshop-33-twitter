// @ts-check
import { assert } from "chai";
suite('App', () => {
   it('should add 2 and 2', () => {
       const input = 2 + 2;
       const output = 4;
       assert.equal(input, output);
   });
});